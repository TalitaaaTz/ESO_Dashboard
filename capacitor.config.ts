import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'br.edu.ufrpe.ficologia.sonda',
  appName: 'Monitoramento Ambiental',
  webDir: 'dist',
}

export default config
